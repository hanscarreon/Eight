<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.eight">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/fblogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_config"
        android:theme="@style/AppTheme"
        tools:ignore="UnusedAttribute">
        <activity android:name=".ViewClasses.MainActivity"/>
        <activity android:name=".ViewClasses.Privacy"/>
        <activity android:name=".ViewClasses.PlayerVid"/>
        <activity android:name=".ViewClasses.LeagueEvent"/>
        <activity android:name=".ViewClasses.Leagues"/>
        <activity android:name=".ViewClasses.Players"/>
        <activity android:name=".ViewClasses.Team"/>
        <activity android:name=".ViewClasses.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>